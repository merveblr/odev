 `toplam` değişkenini 0 olarak başlat

 `sayi` değişkenini 1 olarak başlat

 `sayi` 100'den küçük veya eşit olduğu sürece:
    a. `toplam` = `toplam` + `sayi`
    b. `sayi` değişkenini 1 arttır
 `toplam` değerini ekrana yazdır
